import { ChainId, Role } from "./types";
type Props = {
    address: `0x${string}`;
    roleKey: `0x${string}`;
    /** Specify a block height to fetch a historic role state. Defaults to latest block. */
    blockNumber?: number;
} & ({
    /** pass a chainId to use query against a dev subgraph */
    chainId: ChainId;
} | {
    /** pass your own subgraph endpoint for production use */
    subgraph: string;
});
type FetchOptions = Omit<RequestInit, "method" | "body">;
export declare const fetchRole: ({ address, roleKey, blockNumber, ...rest }: Props, options?: FetchOptions) => Promise<Role | null>;
export {};
