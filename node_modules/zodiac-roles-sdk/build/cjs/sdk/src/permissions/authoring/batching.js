"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.forAll = void 0;
const forAll = (targetAddresses, allow) => {
    const allowArray = Array.isArray(allow) ? allow : [allow];
    return targetAddresses.flatMap((targetAddress) => allowArray.map((allow) => (Object.assign(Object.assign({}, allow), { targetAddress }))));
};
exports.forAll = forAll;
