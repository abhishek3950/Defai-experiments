import { Condition } from "zodiac-roles-deployments";
export declare const conditionId: (condition: Condition) => string;
/**
 * Calculates the create2 storage address of the condition.
 */
export declare const conditionAddress: (condition: Condition) => string;
