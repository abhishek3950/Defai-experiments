"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.decodeRoleKey = exports.encodeRoleKey = void 0;
const ethers_1 = require("ethers");
const encodeRoleKey = (roleKey) => {
    if (roleKey.startsWith("0x") && roleKey.length === 66) {
        // already encoded
        return roleKey;
    }
    return (0, ethers_1.encodeBytes32String)(roleKey);
};
exports.encodeRoleKey = encodeRoleKey;
const decodeRoleKey = (roleKey) => {
    if (roleKey.startsWith("0x") && roleKey.length === 66) {
        return (0, ethers_1.decodeBytes32String)(roleKey);
    }
    try {
        (0, ethers_1.encodeBytes32String)(roleKey);
    }
    catch (e) {
        throw new Error(`Invalid role key: ${roleKey}`);
    }
    return roleKey;
};
exports.decodeRoleKey = decodeRoleKey;
