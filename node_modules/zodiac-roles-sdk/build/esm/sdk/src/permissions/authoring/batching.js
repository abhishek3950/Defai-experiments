export const forAll = (targetAddresses, allow) => {
    const allowArray = Array.isArray(allow) ? allow : [allow];
    return targetAddresses.flatMap((targetAddress) => allowArray.map((allow) => ({ ...allow, targetAddress })));
};
