import { Target } from "zodiac-roles-deployments";
/**
 *  Returns targets granted by `a` that are not granted by `b`
 */
export declare const diffTargets: (a: readonly Target[], b: readonly Target[]) => Target[];
